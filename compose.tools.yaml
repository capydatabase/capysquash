# capysquash — optional development tools (pgAdmin, Filebrowser).
# Overlay for compose.yaml. Both sit behind the `tools` profile, so they only
# start when you ask for them.
#
#   docker compose -f compose.yaml -f compose.tools.yaml --profile tools up -d

x-logging: &logging
  driver: local
  options:
    max-size: "20m"
    max-file: "5"

services:
  pgadmin:
    image: dpage/pgadmin4:latest
    pull_policy: weekly
    profiles: [tools]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    logging: *logging
    environment:
      # pgAdmin rejects reserved domains (.localhost/.local) outright and exits,
      # so this default has to be a real-looking address.
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@capysquash.dev}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-capysquash_pgadmin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
      # Under no-new-privileges pgAdmin cannot bind the privileged port 80 and
      # silently falls back to 8080; pin it so the published port matches.
      PGADMIN_LISTEN_PORT: 8080
    volumes:
      - pgadmin-data:/var/lib/pgadmin
    networks: [capysquash-net]
    ports:
      - target: 8080
        published: "${PGADMIN_PORT:-5050}"
        host_ip: 127.0.0.1
        protocol: tcp
    depends_on:
      postgres-primary:
        condition: service_healthy
    deploy:
      resources:
        limits: { memory: 512M }

  # Browses the whole repository, so it is loopback-only and profile-gated.
  filebrowser:
    image: filebrowser/filebrowser:latest
    pull_policy: weekly
    profiles: [tools]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    logging: *logging
    environment:
      FB_DATABASE: /database/filebrowser.db
      FB_ROOT: /srv
    volumes:
      - ./:/srv
      - filebrowser-data:/database
    networks: [capysquash-net]
    ports:
      - target: 80
        published: "${FILEBROWSER_PORT:-8081}"
        host_ip: 127.0.0.1
        protocol: tcp
    deploy:
      resources:
        limits: { memory: 512M }

volumes:
  pgadmin-data:
  filebrowser-data:
